[package]
name = "robot_app"
version = "0.1.0"
edition = "2024"

[dependencies]
console_error_panic_hook = "0.1.7"
console_log = "1.0.0"
leptos = { version = "0.8.15", features = ["csr"] }
log = "0.4.29"

[lints]
workspace = true

[profile.release]
opt-level = 'z'
codegen-units = 1
lto = true